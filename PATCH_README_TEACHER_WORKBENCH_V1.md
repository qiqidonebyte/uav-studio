# UAV Studio · Teacher Workbench V1

本补丁把 UAV Studio 从“学生独立使用的装调检修仿真软件”推进为可由教师组织实训任务、记录训练过程并查看学生表现的教学平台。

## 覆盖方式

ZIP 为 **DIRECT OVERLAY**。解压后，压缩包根目录直接包含 `backend/`、`frontend/`、`tests/`，没有额外包装目录。将其覆盖到当前 `uav-studio` 仓库根目录即可。

建议覆盖前先备份当前 SQLite 数据库。补丁只新增教学相关数据表，不删除既有用户、飞机、组件或实验记录。后端重启时，现有 `Base.metadata.create_all()` 会自动创建新增表。

## V1 功能

### 角色与导航

- 普通注册账号仍默认为 `student`。
- `teacher` / `admin` 顶部导航在“系统设置”前显示 **教师工作台**。
- `student` 顶部导航显示 **我的实训**。
- 前端路由和后端 API 同时进行角色校验。
- 管理员可在教师工作台的“用户角色”中将普通学生账号提升为教师；用户不能自行注册成教师。

### 教师工作台 `/teacher`

包含：

1. 教学总览：班级数、学生数、任务数、已完成实训、平均分、近期任务。
2. 班级管理：创建班级、生成邀请码、查看学生数与任务数。
3. 实训任务：按班级发布现有故障案例任务，支持开始/截止时间和说明。
4. 实训记录：查看学生状态、成绩、用时、错误次数、提示次数、首次通过及单次过程详情。
5. 学生情况：按学生汇总完成次数、平均分、首次通过率、平均用时、提示与错误操作。
6. 用户角色（仅管理员）：设置 `student / teacher`。

### 学生“我的实训” `/training`

- 输入班级邀请码加入班级。
- 查看教师发布的实训任务。
- 点击开始后创建/恢复 `TrainingRun`。
- 自动进入现有系统调试工作台，并锁定教师指定的故障案例。

### 持久化教学数据

新增 SQLite 数据模型：

```text
ClassroomRecord
ClassEnrollmentRecord
AssignmentRecord
TrainingRunRecord
TrainingEventRecord
```

教学链路：

```text
管理员设置教师身份
        ↓
教师创建班级 / 获取邀请码
        ↓
学生加入班级
        ↓
教师发布故障实训任务
        ↓
学生“我的实训”开始任务
        ↓
TrainingRun 创建
        ↓
原系统调试 / 故障案例工作台
        ↓
过程状态持续写入 TrainingRun
关键步骤写入 TrainingEvent
        ↓
学生提交诊断
        ↓
教师工作台查看成绩与过程
```

### 与 Fault Training V1 的连接

不复制一套新的调试页面。教师任务仍然进入现有 `Debugging.vue` / Fault Training 工作台。

为避免大范围重写成熟的调试页面，本版在 `frontend/src/utils/training.ts` 增加 Assignment Sync Bridge：当 URL 中存在 `run` 和 `scenario` 时，现有评分函数会把案例进度同步至后端；点击“提交诊断”会提交最终结果。自由调试模式没有 `run` 参数，不产生教学任务记录。

## 部署后操作

1. 覆盖补丁文件。
2. 重启 FastAPI 后端，使新增表自动创建。
3. 重新构建前端并部署静态文件。
4. 用 `admin` 登录 → 教师工作台 → 用户角色，把所需账号设为教师。
5. 教师创建班级并把邀请码发给学生。
6. 学生加入班级，教师发布实训任务。

## 当前 V1 边界

- 本版聚焦“班级—任务—TrainingRun—TrainingEvent—教师查看”的教学闭环。
- PX4 飞行验证已有后端记录接口，但 **V1 尚未自动把 FlightLab 的飞行完成状态回写到 TrainingRun**；教师端当前不把这一字段作为任务完成条件展示。
- 暂不包含课表、考试排考、消息通知、学分、证书、AI 评价或学校教务系统对接。
