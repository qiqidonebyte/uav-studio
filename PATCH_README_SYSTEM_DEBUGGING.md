# UAV Studio · System Debugging Workbench Overlay

面向《无人机装调检修》课程增加“系统调试”模块，把现有产品链路扩展为：

`数字装配 → 工程校核 → 系统调试 → 飞行验证 → 实验回放`

## 新增内容

- 顶部导航增加“系统调试”；
- `/debugging` 系统调试工作台；
- 飞控 / IMU / 罗盘 / GNSS 调试；
- M1–M4 电机响应和旋向测试；
- 电池上电电压与现有工程电流裕量联合检查；
- 遥控器校准、链路质量、Failsafe、返航高度与低电量阈值；
- 起飞前机械与环境最终检查；
- 自动生成“调试完成 / 条件通过 / 禁止起飞”结论；
- 标准、GNSS 异常、动力异常、安全保护异常、综合考核 5 个教学预设；
- 按“用户 + 当前飞机”在浏览器保存调试状态；
- 调试检查点历史与 JSON 报告导出；
- 新增纯逻辑单元测试 `debugging-workbench.test.ts`。

## 设计边界

第一版不伪装成真实飞控通信。传感器状态、电机测试和保护参数是课程调试工作台中的教学状态；工程质量、推重比、重心、电流裕量和电池规格直接读取 UAV Studio 当前飞机真实工程模型。

该边界便于下一阶段继续接入：

1. 实体训练机参数模板；
2. 故障注入 / 检修案例；
3. 教师任务与自动评分；
4. MAVLink / 飞控真实数据接口。

## 覆盖方式

将 ZIP 在 UAV Studio 仓库根目录解压覆盖同名文件即可。新增文件不会修改数据库结构，也不需要迁移 SQLite。
