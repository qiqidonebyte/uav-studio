# UAV Studio System Debugging Workbench V3

本覆盖包针对已经确认的系统调试概念图进行重新实现，重点不是增加新功能，而是让真实页面的信息架构、版式和交互顺序与概念图一致。

## 核心变化

- 调试页使用独立的 70px 产品顶部导航，与概念图一致。
- 页面固定为左侧调试流程 / 中央工作台 / 右侧遥测与记录三栏结构。
- 动力系统页面中央顶部采用“3D 无人机 + M1~M4 测试按钮 + 动力参数”布局。
- 中央底部采用“执行机构状态 + 旋向与映射”双面板布局。
- 右侧采用“实时状态 / Pre-Arm / 调试时间轴 / 调试得分 / 操作按钮”布局。
- 继续复用 UAV Studio 已有 DroneScene 与真实 GLB/PBR 组件资产。
- 单电机测试通过现有 TelemetryFrame 驱动数字旋翼动画。
- 电机映射故障场景：M1 指令实际触发 M3；修复后恢复正确响应。
- PX4/Gazebo 尚未真实接入，因此界面明确显示教学模拟 / 待接入，不伪造在线状态。

## 覆盖方法

在当前 `uav-studio` 仓库根目录解压本 ZIP 并覆盖同名文件。

然后执行：

```bash
cd frontend
npm run test
npm run build
```

